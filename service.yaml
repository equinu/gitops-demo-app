apiVersion: v1
kind: Service
metadata:
  name: saas-frontend-demo
  namespace: default
  labels:
    app: saas-frontend
spec:
  type: NodePort
  selector:
    app: saas-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080
